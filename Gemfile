source "https://rubygems.org"
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

# Specify your gem's dependencies in rails_anonymous_controller_testing.gemspec.
gemspec

gem "sprockets-rails"
